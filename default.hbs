<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{meta_title}}</title>
    <link rel="stylesheet" href="{{asset "built/screen.css"}}">
        <script>
        	if(typeof(Storage) !== 'undefined') {
                if (localStorage.getItem('selected-theme') == 'light') {
                    document.documentElement.setAttribute('data-theme', 'light');
                }
                else if (localStorage.getItem('selected-theme') == 'dark') {
                    document.documentElement.setAttribute('data-theme', 'dark');
                }
        	}
        </script>
        {{#is "home"}}
        {{> "newsletter"}}
    <link rel="stylesheet" href="{{asset "built/home.css"}}">
        {{/is}}
    {{ghost_head}}
</head>

<body class="{{body_class}}{{{block "body_class"}}} {{#if @site.description}}is-head-b--a_n{{else}}is-head-b--n--a{{/if}}{{#match @custom.title_font "=" "Elegant serif"}} has-serif-title{{/match}}{{#match @custom.body_font "=" "Elegant serif"}} has-serif-body{{/match}}{{#is "tag"}}{{#if tag.feature_image}} is-head-transparent{{/if}}{{/is}}{{#match @custom.header_style "Accent color"}} is-head-brand{{/match}}{{#match @custom.header_style "Dark"}} is-head-dark{{/match}}">

        {{#if @member.paid}}
        {{> "site-structure/social-shit"}}
        {{else if @member.paid}}
        {{#is "home"}}
        {{/is}}
        {{/if}}
    <div class="gh-site">
        <header id="gh-head" class="gh-head gh-outer">
            <div class="gh-head-inner gh-inner">
                <div class="gh-head-brand">
                    <div class="gh-head-brand-wrapper">
                        <a class="gh-head-logo" href="{{@site.url}}">
                            {{#if @site.logo}}
                                {{#if @custom.white_publication_logo_for_transparent_header}}
        <img class="logo-light" src="{{@site.logo}}" alt="{{@site.title}}">
        <img class="logo-dark" src="{{@custom.white_publication_logo_for_transparent_header}}" alt="{{@site.title}}">
                                    {{else}}
                            <img src="{{@site.logo}}" alt="{{@site.title}}">
                                {{/if}}
                            {{else}}
                                {{@site.title}}
                            {{/if}}
                        </a>
                    </div>
                    <button class="gh-burger"></button>
                </div>

                <nav class="gh-head-menu">
                    {{navigation}}
                    {{#unless @member}}
                        <a class="gh-head-btn gh-btn" href="/registro" data-portal="signup">{{ t "I want the info" }}</a>
                    {{else}}
                        <a class="gh-head-btn gh-btn" href="#/portal/account" data-portal="account">{{ t "Account" }}</a>
                    {{/unless}}
                </nav>

                <div class="gh-head-actions">
                    <button class="gh-search gh-icon-btn" data-ghost-search>{{> "icons/search"}}</button>
                    <div class="it-dark-light">
                    <div class="icon-dark">
                        {{> icons/light}}
                    </div>
                    <div class="icon-light">
                        {{> icons/dark}}
                    </div>
                    </div>
                </div>
            </div>
        </header>

        {{!-- {{navigation}} --}}

        {{{body}}}

        <footer class="gh-foot gh-outer">
            <div class="gh-foot-inner gh-inner">                     
                    <section class="gh-subscribe">
                        {{#if @member.paid}}
                        <h4 class="gh-subscribe-title">{{ t "We are here" }}</h3>
                                <div class="gh-subscribe-description">{{ t "Contact us paid"}}</div>
                        {{else if @member}}
                            <h4 class="gh-subscribe-title">{{ t "Want trading" }}</h3>
 {{else}}
                        <h4 class="gh-subscribe-title">{{ t "Subscribe to" }}</h3>
                                <div class="gh-subscribe-description">{{ t "Newsletter description"}}</div>

                            <button class="gh-subscribe-btn gh-btn" data-portal="signup">{{ t "Subscribe now" }}</button>
                {{/if}}
                    </section>

                <nav class="gh-foot-menu">
                    {{navigation type="secondary"}}
                </nav>

                <div class="gh-copyright">
                    {{#unless @custom.footer_text}}
                {{ t "from the 🌎 with 🧡 by" }} <a href="https://matiasisea.com" title="ir a la web de Matías" target="_blank" rel="noopener">Matías Isea</a>
                <p> Criptonautas&nbsp;{{ t "is licensed under" }}  <a href="http://creativecommons.org/licenses/by-nc-sa/4.0/?ref=chooser-v1" target="_blank" rel="license noopener noreferrer" >Creative Commons</a><br>({{ t "you can share it for free" }})</p>

                    {{else}}
                        {{@custom.footer_text}}
                    {{/unless}}
                </div>
            </div>
        </footer>
    </div>

    {{#is "post, page"}}
        {{> "pswp"}}
    {{/is}}

{{> "site-structure/site-scripts"}} 
        {{{block "scripts"}}}
{{ghost_foot}}
</body>

</html>