<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{meta_title}}</title>
    <link rel="stylesheet" href="{{asset "built/screen.css"}}">
<link rel="preconnect" href="https://comunidad.criptonautas.co">
        <script>
        	if(typeof(Storage) !== 'undefined') {
                if (localStorage.getItem('selected-theme') == 'light') {
                    document.documentElement.setAttribute('data-theme', 'light');
                }
                else if (localStorage.getItem('selected-theme') == 'dark') {
                    document.documentElement.setAttribute('data-theme', 'dark');
                }
        	}
        </script>
        {{#if @member}}
        {{else}}
        {{#is "home"}}
        {{> "newsletter"}}
    <link rel="stylesheet" href="{{asset "built/home.css"}}">
        {{else}}
        {{/is}}
        {{/if}}
    {{ghost_head}}
<script type="module">
  import Typebot from "https://cdn.jsdelivr.net/npm/@typebot.io/js@0.0.14/dist/web.js";

  Typebot.initBubble({
    typebot: "hi",
    apiHost: "https://bot.criptonautas.co",
    theme: {
      button: { backgroundColor: "#5107ed", iconColor: "#FFFFFF" },
      '.text-base': { fontSize: "1.5rem" },
    },
  });
</script>
</head>

<<<<<<< HEAD
<body class="{{body_class}}{{{block "body_class"}}} {{#if @site.description}}is-head-b--a_n{{else}}is-head-b--n--a{{/if}}{{#is "tag"}}{{#if tag.feature_image}} is-head-transparent{{/if}}{{/is}}{{#match @custom.header_style "Accent color"}} is-head-brand{{/match}}{{#match @custom.header_style "Dark"}} is-head-dark{{/match}}">
    <div class="gh-site">
        <header id="gh-head" class="gh-head gh-outer">
            <div class="gh-head-inner gh-inner">
                <div class="gh-head-brand">
                    <div class="gh-head-brand-wrapper">
                        <a class="gh-head-logo" href="{{@site.url}}">
                            {{#if @site.logo}}
        <img class="logo-light" src="{{@site.logo}}" alt="{{@site.title}}">
        <img class="logo-dark" src="{{@custom.white_publication_logo_for_transparent_header}}" alt="{{@site.title}}">
                            {{else}}
                                {{@site.title}}
                            {{/if}}
                        </a>
                    </div>
                </div>
                    <button id="gh-burger" class="gh-burger"></button>
                <nav class="gh-head-menu">
                    {{navigation}}
                </nav>
                <div class="gh-head-actions">
                    <div class ="actions-container">
                                            {{#unless @member}}
                        <a aria-label="signup-button" title="start here" class="gh-head-btn gh-btn shadow" href="{{@site.url}}">{{ t "Subscribe" }}</a>
                    {{else}}
                        <a aria-label="signin-button" title="continue"  class="gh-head-btn gh-btn shadow" href="{{@site.url}}/player">{{ t "Account" }}</a>
                    {{/unless}}
                    <div class="nav-icons">
                    <button class="gh-search gh-icon-btn" data-ghost-search>{{> "icons/search"}}</button>
                    <div class="it-dark-light gh-icon-btn">
                    <div class="icon-dark">
                        {{> icons/light}}
                    </div>
                    <div class="icon-light">
                        {{> icons/dark}}
                    </div>
                    </div>
                </div>
=======
<body class="{{body_class}}{{{block "body_class"}}} is-head-{{#match @custom.navigation_layout "Logo on the left"}}left-logo{{else match @custom.navigation_layout "Logo in the middle"}}middle-logo{{else}}stacked{{/match}}{{#match @custom.title_font "=" "Elegant serif"}} has-serif-title{{/match}}{{#match @custom.body_font "=" "Elegant serif"}} has-serif-body{{/match}}{{#is "tag"}}{{#if tag.feature_image}} is-head-transparent{{/if}}{{/is}}{{#match @custom.header_style "Accent color"}} is-head-brand{{/match}}{{#match @custom.header_style "Dark"}} is-head-dark{{/match}}">
<div class="gh-site">

    <header id="gh-head" class="gh-head gh-outer">
        <div class="gh-head-inner gh-inner">
            <div class="gh-head-brand">
                <div class="gh-head-brand-wrapper">
                    {{#is "home"}}<h1 class="gh-head-logo">{{/is}}
                    <a {{^is "home"}}class="gh-head-logo"{{/is}} href="{{@site.url}}">
                        {{#if @site.logo}}
                            <img src="{{@site.logo}}" alt="{{@site.title}}">
                            {{#if @custom.white_publication_logo_for_transparent_header}}
                                <img src="{{@custom.white_publication_logo_for_transparent_header}}" alt="{{@site.title}}">
                            {{/if}}
                        {{else}}
                            {{@site.title}}
                        {{/if}}
                    </a>
                    {{#is "home"}}</h1>{{/is}}
                </div>
                <button class="gh-search gh-icon-btn" aria-label="Search this site" data-ghost-search>{{> "icons/search"}}</button>
                <button class="gh-burger"></button>
            </div>

            <nav class="gh-head-menu">
                {{navigation}}
                {{#unless @site.members_enabled}}
                    {{#match @custom.navigation_layout "Stacked"}}
                        <button class="gh-search gh-icon-btn" aria-label="Search this site" data-ghost-search>{{> "icons/search"}}</button>
                    {{/match}}
                {{/unless}}
            </nav>

            <div class="gh-head-actions">
                {{#unless @site.members_enabled}}
                    {{^match @custom.navigation_layout "Stacked"}}
                        <button class="gh-search gh-icon-btn" aria-label="Search this site" data-ghost-search>{{> "icons/search"}}</button>
                    {{/match}}
                {{else}}
                    <button class="gh-search gh-icon-btn" aria-label="Search this site" data-ghost-search>{{> "icons/search"}}</button>
                    <div class="gh-head-members">
                        {{#unless @member}}
                            {{#unless @site.members_invite_only}}
                                <a class="gh-head-link" href="#/portal/signin" data-portal="signin">Sign in</a>
                                <a class="gh-head-btn gh-btn gh-primary-btn" href="#/portal/signup" data-portal="signup">Subscribe</a>
                            {{else}}
                                <a class="gh-head-btn gh-btn gh-primary-btn" href="#/portal/signin" data-portal="signin">Sign in</a>
                            {{/unless}}
                        {{else}}
                            <a class="gh-head-btn gh-btn gh-primary-btn" href="#/portal/account" data-portal="account">Account</a>
                        {{/unless}}
>>>>>>> efacc93842633ea8cb7ac4a541cc5483c8b479a5
                    </div>
                </div>
            </div>
        </header>
            </div>
        {{{body}}}
{{^is "page"}}
        <footer class="gh-foot gh-outer shadow">
            <div class="gh-foot-inner gh-inner">                     

  {{#get 'posts'}}
    {{#foreach posts visibility='all'}}
        {{/foreach}}
  {{/get}}
    <section>
                {{#if @member.paid}}
                        <h4 class="gh-subscribe-title">{{ t "We are here" }}</h3>
                                <div class="gh-subscribe-description">{{ t "Contact us paid"}}</div>
                                <a class="gh-subscribe-btn gh-btn" href="https://comunidad.criptonautas.co/c/criptonautas/4">{{ t "Post forum" }}</a>
                        {{else if @member}}
                            <h4 class="gh-subscribe-title">{{ t "Shared analysis?" }}</h3>
                            <div class="gh-subscribe-description">{{ t "Padawans description"}}</div>
                                                        <a class="gh-subscribe-btn gh-btn" href="https://discord.criptonautas.co">{{ t "Learn from Criptonauts" }}</a>
                        {{else}}
                        <h4 class="gh-subscribe-title">{{ t "Subscribe to" }}</h3>
                                <div class="gh-subscribe-description">{{ t "Newsletter description"}}</div>

                            <a class="gh-subscribe-btn gh-btn" href="{{@site.url}}">{{ t "Subscribe now" }}</a>
                {{/if}}
                    </section>
            </div>
        </footer>
{{/is}}
<div class="footer-criptonauta">
                        <nav class="gh-foot-menu">
                    {{navigation type="secondary"}}
                </nav>
            <div class="it-foot-social">
                <a href="https://comunidad.criptonautas.co/" class="discourse" title="{{t "Discourse"}}" target="_blank" rel="noopener" aria-label="{{t "Discourse"}}">{{> "icons/discourse"}}</a>
                {{#if @site.twitter}}
                <a href="{{twitter_url @site.twitter}}" title="{{t "Twitter"}}" target="_blank" rel="noopener" aria-label="{{t "Twitter"}}">{{> "icons/twitter"}}</a>
                {{/if}}
                <a href="https://github.com/somos-criptonautas/" class="github" title="{{t "Github"}}" target="_blank" rel="noopener" aria-label="{{t "Github"}}">{{> "icons/github"}}</a>
                <a href="{{@site.url}}/rss/" class="rss" title="{{t "RSS"}}" target="_blank" rel="noopener" aria-label="{{t "RSS"}}">{{> "icons/rss"}}</a>
            </div>
                <div class="gh-copyright">
                    {{#unless @custom.footer_text}}
                <p>Criptonautas&nbsp;{{ t "is licensed under" }}  <a href="https://comunidad.criptonautas.co/t/actualizamos-la-licencia-de-criptonautas/3864" rel="license">Polyform</a><br>{{ t "with 🧘‍♂️" }} <a href="https://matenauta.co" title="matenauta's lab" target="_blank" rel="noopener">matenauta</a></p>

                    {{else}}
                        {{@custom.footer_text}}
                    {{/unless}}
                </div>
    </div>
</div>
    {{#is "post, page"}}
        {{> "pswp"}}
    {{/is}}

{{> "site-structure/site-scripts"}} 
        {{{block "scripts"}}}
{{ghost_foot}}
</body>

</html>